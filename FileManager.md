# FileManager

An amazingly useless tool!

Console

```

list a folder:
The user enters a folder's path and it hows
a list of files in the folder.
info on on a file:
User enters the folder and info 
returns whats in the folder.

mkdir make a new folder:
The user makes a new directory.

rename a file:
The USer renames a file.

copy, move, delete files:
The user can copy, move or delete
files within directory.

quit the file manager shell:
Exit the console.
```